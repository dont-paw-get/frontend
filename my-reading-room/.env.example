# 로컬 개발 시에는 비워두면 Vite proxy(vite.config.js)가 /api → localhost:8000 으로 프록시합니다.
# 별도 도메인의 백엔드를 직접 호출하려면 아래 값을 채우고 .env.local 로 복사해서 사용하세요.
# 예: VITE_API_BASE_URL=https://api.xxx.com/api/v1
VITE_API_BASE_URL=
